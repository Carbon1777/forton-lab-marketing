requests==2.32.3
python-frontmatter==1.1.0
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
anthropic>=0.39,<1.0
python-telegram-bot[job-queue]==21.7
replicate>=1.0.7,<2
PyYAML==6.0.3
# Phase 9-05 — identity anchor (Facenet512 + cosine ≥0.85, CHAR-06).
# Install in a separate Python 3.12 venv (.venv-face) — TensorFlow has no wheel for 3.14.
# Also requires `tf-keras` (TF 2.16+ uses Keras 3, retina-face/deepface need legacy Keras shim).
deepface==0.0.100
tf-keras
# Phase 10 — voice selection (ElevenLabs SDK, verified 2026-05-07 per 10-RESEARCH.md §Standard Stack).
elevenlabs>=2.46.0,<3
# Phase 11 — compositor frame ops (PIL.Image / ImageDraw / ImageFont primitives).
# Upper bound <13 covers current venv (12.2.0) + future minor releases; Phase 11
# production code uses only stable Image/ImageDraw API, safe across 10/11/12.
pillow>=10.4,<13

